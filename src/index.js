import React from 'react';
import ReactDOM from 'react-dom';

// import './styles.css';

function App() {
  return (
    <div className="App">
      <h1>New AIMprove game, but optimized</h1>
      <h2>Lets get this thing started and live!</h2>
    </div>
  );
}

const rootElement = document.getElementById('root');
ReactDOM.render(<App />, rootElement);
